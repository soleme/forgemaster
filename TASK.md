# Forge Master 공략 사이트 작업 목록

## 완료한 작업

- Forge Master 공략 사이트를 `Vite + React + TypeScript`로 구성했다.
- Binance 스타일을 참고한 디자인 방향을 적용했다.
  - 거의 검정에 가까운 다크 캔버스
  - 노란색 포인트 컬러와 주요 CTA
  - 금융/거래 플랫폼 느낌의 촘촘한 카드와 테이블
  - 작은 반경의 카드와 버튼
  - 다크 본문 뒤를 닫는 밝은 푸터
- `Forge Master Guidebook_Ver08.0.pdf`를 분석했다.
- PDF 23페이지 전체를 이미지로 추출해 `public/guidebook/`에 넣었다.
- `src/data.ts`에 공략 데이터를 구조화했다.
- `src/main.tsx`에 메인 공략 화면을 구현했다.
- `src/styles.css`에 반응형 스타일을 구현했다.
- 다음 공략 섹션을 추가했다.
  - 시작 가이드
  - 옵션
  - 장비와 대장간 레벨
  - 스킬
  - 펫
  - 룬
  - 탈것
  - 비행기 모드
  - 클랜과 클랜전
  - 리그 보상
  - 과금 효율
  - 디딤돌
  - PDF 원문 페이지 뷰어
- 공략 데이터 검색 기능을 추가했다.
- 다음 필터를 추가했다.
  - 옵션 필터
  - 대장간 레벨 필터
  - 리그 랭크 필터
- 리그 보상 정렬을 추가했다.
  - 태엽 높은순
  - 스킬티켓 높은순
- 모바일 햄버거 메뉴를 추가했다.
- `.gitignore`를 추가해 생성물과 로컬 전용 파일을 제외했다.
- `npm run build`로 빌드를 검증했다.
- git 저장소를 초기화하고 GitHub에 초기 사이트를 푸시했다.
  - 저장소: `https://github.com/soleme/forgemaster`
  - 브랜치: `main`
  - 커밋: `b39695f Build Forge Master guide site`
- Vercel 프로덕션 배포를 완료했다.
  - 프로덕션 주소: `https://forgemaster-pi.vercel.app`
  - 최신 배포 URL: `https://forgemaster-62m0yx9z1-soleme-2061s-projects.vercel.app`
  - 최신 배포 ID: `dpl_2WrgpBJ476r8its4rtCExAevdUQC`

## 현재 한계

- PDF의 이미지형 표와 일부 스크린샷 기반 내용은 구조화 데이터로 완전히 옮기지 못했고, 원문 이미지 뷰어로 보존했다.
- 스킬 리스트는 원문이 이미지/표 중심이라 현재는 요약 형태다.
- 클랜전 개인 보상과 단체 보상 표는 전체 전사하지 않고 요약했다.
- 리그 보상은 PDF 전체 행이 아니라 대표 구간 중심으로 들어가 있다.
- 아직 라우트 기반 페이지 구조가 아니고, 하나의 긴 단일 페이지 앱이다.
- 새 PDF 버전이 나왔을 때 자동으로 데이터를 추출하는 파이프라인은 없다.
- 단위 테스트나 브라우저 스크린샷 테스트는 아직 없다.
- Vercel 배포는 완료됐고, GitHub 권한 연결 후 재배포 시 이전 저장소 연결 오류는 재발하지 않았다.

## 다음 작업

1. PDF의 남은 이미지형 섹션을 구조화 데이터로 전환한다.
   - 스킬 리스트
   - 스킬 확률 테이블 예시
   - 클랜전 개인 보상
   - 클랜전 단체 보상
   - 리그 보상 전체 행

2. 앱을 유지보수 가능한 컴포넌트로 분리한다.
   - 내비게이션
   - 히어로
   - 검색
   - 데이터 테이블
   - 공략 카드
   - 원문 페이지 뷰어

3. 라우트 기반 페이지를 추가한다.
   - `/options`
   - `/gear`
   - `/skills`
   - `/runes`
   - `/league`
   - `/source`

4. 검색 기능을 개선한다.
   - 검색어 하이라이트
   - 섹션별 검색
   - 결과 없음 안내
   - 한국어 띄어쓰기 차이를 고려한 검색

5. 테이블 기능을 개선한다.
   - 대장간 레벨 표 컬럼 정렬
   - 리그 보상 전체 필터
   - 모바일에서 넓은 표를 카드형으로 표시
   - 데스크톱에서 테이블 헤더 고정

6. 계산기를 추가한다.
   - 옵션 효율 계산기
   - 대장간 업그레이드 계획 도우미
   - 리그 보상 비교 도우미

7. 원문 추적성을 강화한다.
   - 각 데이터 섹션에 PDF 페이지 번호를 연결
   - 각 섹션에 "원문 페이지 보기" 링크 추가

8. 배포 설정을 보강한다.
   - Git push 시 자동 배포가 실제로 트리거되는지 확인
   - `README.md`에 배포 방법 기록

9. 프로젝트 문서를 추가한다.
   - `README.md` 작성
   - 로컬 실행 방법 기록
   - 새 가이드북 버전 업데이트 절차 기록

10. 검증 자동화를 추가한다.
    - CI에서 `npm run build` 실행
    - GitHub Actions 워크플로 추가
    - 필요하면 Playwright 기반 화면 스모크 테스트 추가

## 유용한 명령어

```bash
npm install
npm run dev
npm run build
```

로컬 개발 주소:

```text
http://localhost:5173/
```
